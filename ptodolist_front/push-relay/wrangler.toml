name = "ptodolist-push-relay"
main = "src/index.js"
compatibility_date = "2026-08-01"

[vars]
FIREBASE_PROJECT_ID = "ptodolist"

# 배포 전 시크릿 등록 (Firebase 콘솔 > 프로젝트 설정 > 서비스 계정 > 새 비공개 키):
#   npx wrangler secret put SERVICE_ACCOUNT_JSON   ← 키 JSON 파일 내용 전체를 붙여넣기
